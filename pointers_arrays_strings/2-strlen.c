#include "main.h"

/**
 * _strlen - returns the length of a string.
 * @s: input string.
 * Return: length of a string.
 */
int _strlen(char *s)
{
	size_t length = 0;

	while (*str++)
		length++;

	return (length);
}
